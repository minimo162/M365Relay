# 起動状態表示と診断ログの配布版検証

状態: この変更の限定検証は合格。アプリ全体の目標はpartial。

29f233491ade由来の候補ZIPをビルドし、Windows配布8検査に成功。旧サーバー終了確認後、展開済みRun.cmdで起動した。新PID44264、exec93483。ログ保存先が起動表示に出ることを確認。

実M365へ診断用の固定文字列を含む通常回答要求を送信し、HTTP200、11534ms、指定した数字の完全一致を確認した。起動ウィンドウの追加出力は「M365で処理中です。」「応答をVS Codeへ返しました。」の2行。大量のbackend_timing JSONは既定表示に出ない。

ローカルログにはaccepted/backend_timing/response_returnedが保存された。依頼内の検証用識別文字列、認証値、回答本文は含まれていないことを比較で確認した。エラー保存や容量制限は単体テストで確認しており、実ディスク満杯による故障注入はしていない。

証拠: `.local/run-status-package/smoke-result.json`。実ログは状態フォルダーの `logs/run-796bd991-d7d4-4c9b-b962-1d5e1be54691.jsonl`。候補ZIP: `dist/run-status-candidate/M365Relay-0.2.16-win-x64-29f233491ade.zip`。

現在の待受はこの新配布版。起動ごとのログは2MiB上限、旧ログの自動削除なし。配布8検査と単体119件の成功を、長期間・複数業務の信頼性や全初回操作の証明に代えない。
