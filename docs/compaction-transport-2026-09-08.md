# 実VS Codeの要約要求での応答形式逸脱

状態: partial。readiness-20260908は終了。修正成果物は固定検証成功だが、変更後再読を省略。対策プロンプトの簡易実M365検証は成功、実VS Code圧縮経路は未検証。

入力欄安定判定を反映したサーバーPID40712、exec session26192に対し、独立通常チャット6228654から固定の複数ファイル修正課題を開始した。追加指示は与えていない。

SPEC.mdと実装3ファイルの読取が進んだ一方、各読取後に通常要求より約6,266文字大きい要求が入り、約29秒かかってm365_response_invalidになる例がある。

失敗した要求c695b9b7-52e4-4825-b79d-4c085fdce50bと4799cf4c-69e0-4ecc-8a02-c23e0851f03aについて、専用Edgeの残された要求タブを読み取り確認した。ページ内の要求IDで対象を識別し、assistant専用DOMだけを調べた。どちらも会話の引継ぎ要約を通常のMarkdown文章として返し、BRIDGE_FINAL_V2と終端行がなく、コードブロックもなかった。要約には実行済み読取と未完了の修正が記述されていた。

したがって、少なくともこの2件は入力消失ではなく、圧縮要求への応答が搬送形式を守らない問題である。要求ログのsummary_request:falseは固定英語文言の検出だけに依存しており、要約要求がなかった証拠にはならない。成功率集計ではこのフラグだけで分類しない。

対策候補として、会話要約もBRIDGE_FINAL_V2の本文に格納し、要約の見出し指定と外側の通信形式を区別する指示をプロンプトに追加した。通常文章を無条件で受理するパーサー変更は行っていない。構文検査25JS/4JSON成功はプロンプトの実効性を証明しない。進行中の観測を継続し、終了後に変更を反映して実要約経路を再検証する。

再現用の読取スクリプト: `.local/input-events/inspect-failed.mjs`。残存要求タブを読んだだけで、入力・送信・再送・タブ閉鎖はしていない。

## 実作業の終了結果

追加指示なしで実装3ファイルを編集して最終回答まで到達した。固定verify.mjsは合計、ゼロ、既定数量、active抽出、カテゴリ順、識別子、notes、負数量拒否の検査に成功。SPEC.mdとverify.mjsのSHA256は開始前と一致。ただし最終回答自身が「変更後の再読は未実施」と明記しており、要求全体の完走には数えない。テストは観測者が別途実行した。

画面表示5分29秒、受理開始18:16:44.753Zから最終応答18:22:13.702Z。15要求中8成功（7ツール応答と最終応答）、7形式エラー。backend合計326489ms、そのうちエラー要求228406ms。3ファイル変更+30/-3。試験集計は `.local/input-events/readiness-task-results.json`。

## 対策後の限定確認

処理終了と旧サーバー終了を確認後、92095a1のプロンプトを読み込むサーバーを起動。PID36956、exec84010。

同じ種類の「要約だけを返す、Chronological Review等の見出しで始める」指示を含む架空の引継ぎ課題を、実M365へ1回送信。HTTP200、12738ms。搬送形式が検証を通り、識別子SUMMARY-492、変更禁止のSPEC.md/verify.mjs、実行済み読取と残りの編集・再読が保持された。記録は `.local/input-events/summary-smoke-result.json`。

これは短い合成要約を実M365経由で送った検証であり、VS Codeが実際に生成する長い圧縮要求の互換性・圧縮頻度・速度改善は未確認。次は同一条件の実VS Code課題で比較する。
