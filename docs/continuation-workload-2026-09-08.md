# 同じチャットでの集計とCSV追加

状態: 限定した継続試験。全体の実用化目標は未完了。

配布版2aefc980b553をRun.cmdで起動し、実VS CodeとM365画面を使用。専用workspaceのcontinuation-v9を対象に、ファイル操作のみを依頼し、実行検査は観測側で行った。モデルによる自律的なテスト実行の証拠ではない。

| 段階 | 実画面の時間 | 結果 |
|---|---:|---|
| 初期集計 | 1分2秒 | total、ID・note保持、数量0・省略・小数、負数拒否、非破壊の検査に合格 |
| カテゴリ集計追加 | 1分24秒 | 通常カテゴリは合格、任意文字列という条件のうち`__proto__`で失敗 |
| 失敗の指摘後 | 1分17秒 | Object.create(null)へ修正。再読取を実画面で確認、段階2の実検査に合格 |
| CSV追加 | 1分33秒 | 初回で通常CSVと既存集計の検査に合格。追加検査では負の数量を拒否しなかった |
| CSV条件明示後 | 1分22秒 | 負数の検証を追加し再読取。既存集計・CSV・追加境界検査がすべて合格 |

指摘後の修正中に実画面の「Compacted conversation」を確認。その後の再読取とCSV追加まで継続した。圧縮を含む限定例であり、長期の任意業務の信頼性を証明しない。

CSVは空配列、カンマ、引用符、CR・LF・CRLF、日本語、絵文字、リテラルのHTML実体参照、空白、分数量、入力非破壊の検査が通った。負数検証は「前段階と同じ計算」という依頼だけでは転用されなかったため、CSVでもactive負数を拒否しinactive負数を無視する条件を明示して追加依頼した。これを初回無介入の合格に含めない。

SPEC.mdのSHA256は段階2修正後も4C5CF6242628BA16FEB88A95570A6FA0A7F62C28AA5F85D0A746081907FB5DAB。段階2まで編集禁止だったcsv.mjsも初期ハッシュを維持していた。

CSV条件明示後も会話圧縮とcsv.mjsの再読取を実画面で確認し、最終回答まで完了。SPEC.mdは最終検査でも同じハッシュ。観測者による実装の直接修正はない。集計の失敗指摘1回とCSV条件の明示1回を必要とした。

観測用検査は.local/input-events/verify-continuation.mjsとverify-csv-extra.mjs。実ランのメタデータは専用stateのlogs/run-aeb26bd2-1d59-48c6-a1d3-f5b2c734fd94.jsonl。本文・トークンを本記録に保存しない。

本体のnode --testは121件合格。これはライブ継続試験の代わりではない。新規起動画面の配置、未認証からの初回導線、より広い作業、持続的なツール回数制限は引き続き未完了。
