# 新規会話ごとのGPT 5.6 Think Deeper選択

ユーザーから、新規チャットは毎回選び直す必要があると確認。実画面のモデルセレクターでも新規タブは自動、GPTメニューにGPT 5.6 Think Deeperを確認した。

CLIの実M365バックエンドに送信前選択を追加。新しい空の会話を確保した後、モデルメニューを開きGPTの項目から正式名を選ぶ。ヘッダーの表示に加え、メニューを再び開き正式名のaria-checked=trueを確認し閉じる。送信直前にもヘッダーを確認する。選択不可・曖昧・タイムアウトは未送信で停止し、自動への代替はしない。

初回実試験では、選択後に親メニュー名がGPTからGPT 5.6 Think Deeperへ変わるため確認に失敗し、未送信で停止した。実DOMを確認してその表示変化に対応した。

修正後の実新規タブで90695文字の既存catalog51課題に合格。正式名チェックを通過した要求f5af311e-214e-43af-9c2c-2913e3eee0ecは全体11733ms、model_select665ms。ツール名と全引数の完全一致を確認。直前のモデル未確認の9564ms走行を比較値として扱わない。

本体125テスト、29JS/4JSON構文検査成功。モデル選択不能と送信直前のモデル変更時に、送信および台帳予約が発生しないことを検査した。選択確認は画面表示の証拠でありサービス内部モデル実装の独立証明ではない。既存Auto課題も合格しているため、この基本課題だけでは正確性向上を主張できない。複数段階の比較は残る。
