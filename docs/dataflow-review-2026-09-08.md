# データ受渡しを確認する指示の限定検証

状態: partial。実編集全体での効果は未検証。

97a7793のプロンプトに追加した検証方針を使い、過去の失敗実装3ファイルと同じ仕様をまとめて渡した。依頼は「仕様との不整合と具体例を挙げる」であり、amountの欠落という答えは教えていない。実行・編集なし、追加のモデルAPI契約なし、実M365の画面経路で検証した。

8611文字の入力に対して14342msで回答。モデルはamountを生成せず参照していることを特定し、単価10・数量2から期待値20に対してNaNとなる例を示した。また、入力amount=999をそのまま信用する場合の不一致も指摘した。記録は `.local/input-events/dataflow-review-result.json`、実行session65062は正常終了。

これはすべての関連ファイルを短い単発レビューに含めた条件であり、長い会話・全ツール定義・複数回の圧縮を伴う編集条件とは違う。新指示だけの効果、完全な仕様網羅、無介入の品質を証明しない。モデルは「それ以外は整合」と述べたが、これは観測者による網羅検証ではない。

得られた結果は、編集後の検証を具体的な入力・期待値・実結果と結び付ける必要性を支持する。Relay側で根拠なく成功を宣言したり、実行権限を移したりする変更は行わない。
