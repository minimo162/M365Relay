# VS Code会話予算28,000の比較準備

状態: partial、設定のみ。実行効果は未検証。製品既定は24,000のまま。

固定課題では約10〜11万文字の要求が繰り返され、51ツールの定義約6.4万文字が会話予算を消費する。インストール済みクライアントはツール分を先に差し引き、残りの約90%で会話を構成する。simple圧縮設定は今回の背景経路で軽量化を確認できなかった。

次の比較では専用プロファイルのmaxInputTokensだけを24,000から28,000へ変更した。maxOutputTokensは8,000を維持。元の数値だけを `.local/input-events/budget-28k-before.json` に保存し、認証ヘッダー等は記録していない。通常プロファイルと製品既定には変更なし。

この数値は実M365のコンテキスト容量を確認したものではなく、クライアントの会話予算を比較するための試験値である。Relayのエスケープ・説明文を含む120,000文字の送信前上限は維持し、超過は送信前に拒否する。余裕を増やした結果、圧縮が遅すぎて上限超過になる可能性も評価対象に含む。

次に同じ仕様・初期欠陥の課題で、入力上限超過、要約回数、文字列忠実性、固定および追加検査、全体時間を比較する。短縮だけで採用せず、完走と品質が必要。設定ファイルの書換えだけでは実際のクライアントへの反映を証明しない。常駐サーバーとテンプレートはこの比較準備では再起動・変更していない。

## budget-v5結果

再読み込み後、独立窓の入力が表示されなかったため親VS Codeチャットから開始。入力・送信を確認し、同じ初期欠陥で追加指示なしの試験を行った。

初回受理19:29:53.577Z、通常の最終応答19:31:57.356Z（画面2分4秒）。9ツール応答と通常最終応答の後、113480文字の背景要約相当要求が19:32:31.303Zに終了した。全11要求成功、backend合計155620ms。全要求終了まで約158秒と、操作上の完了124秒を区別する。記録は `.local/input-events/budget-v5-results.json`。

固定verifyと単価3・数量0.5=1.5の追加検査が成功。SPEC/verifyハッシュ不変。normalizeとreportは再読したが、calculateの変更後再読を省略して終了したため依頼全項目の完走には数えない。送信上限超過はなし。

24kのfidelity-v3（画面6分58秒）より短縮したが、単一比較、窓の表示経路の違い、再読数の違いがある。製品既定の変更前に再現確認が必要。28kは専用プロファイルの試験値として保持し、製品既定24kはまだ変更していない。
