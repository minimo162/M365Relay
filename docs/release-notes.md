# M365Relay 0.2.23 候補版

## 現在の候補と検証範囲（2026-09-08）

この版は検証中です。公開済み0.2.22とは異なり、まだ自動更新の配布先には反映していません。

- 文書処理を同梱Python 3.13.15とopenpyxl・et_xmlfile・pypdf・pypdfium2・Pillowへ移行します。OfficeCLI/LiteParseは含めません。Pillowの任意のAVIFコーデックとOCRは含めません。
- python-pptxとlxmlは利用者の方針により同梱しません。Word/PowerPointの作成とOfficeによる再計算・PDF出力には、デスクトップ版Officeが必要です。
- VS Codeへ申告する入力予算は256,000トークンです。これはM365の入力欄や実際のモデルコンテキストを拡張する設定ではありません。ツール定義と会話はTXT添付で渡し、入力欄へ送る本文は120,000文字以内で検証します。
- レビュー回答内のコードブロックが転送用の外枠を壊さないよう、外側の長いフェンスと具体例を明示します。コロンやコード本文は変更しません。

1639943候補では、Windows配布検査10件、同梱PythonのPDF/Excel処理とPillow画像操作が成功しています。回答の囲み修正後の実M365試験は、コード例を含むレビュー2回と文章のみのレビュー1回で正常受信を確認しました。

実VS CodeではPDFからExcelの作成・再計算・読戻し・PDF/PNG出力を確認し、別の画像読取依頼ではviewImageと最終回答も確認しました。最初の複合依頼では画像確認前に回答を終えたため、無介入で全工程を完走したとは扱いません。会社端末・テナント、未認証の初回起動、一般的な長文作業の安定性は未検証です。

詳細: [Python構成と実機結果](python-runtime.md)、[レビュー回答の囲み検証](review-answer-framing.md)。以下は過去の候補版の記録であり、現在の入力予算や検証状態とは異なります。

---

# M365Relay 0.2.16 候補版

## 2026-09-08 チェックポイント

- 起動ファイルと本体を分離したローカルキャッシュ・自動更新、ハッシュ検証と前回版へのフォールバック。
- 履歴付きチャットの最大化、フォント16・ズーム1、専用環境でのワークスペース信頼無効化、実パスによる履歴保存先の固定。
- GPT 5.6 Think Deeperを新規会話ごとに選択し、送信前に確認。
- 0.2.23候補: OfficeCLI/LiteParseを外し、固定ハッシュのPython埋め込み版と文書ライブラリへ移行。PDF座標・描画、Excel読書きに対応。Office再計算/描画とWord/PowerPoint作成はインストール済みOfficeを使用。OCRは無効。
- PNG/JPEG data URLを検証して画像添付。アップロード前から台帳へ記録し、添付状態を送信直前にも確認。

実機では画像付きHTTP要求の往復、同一要求の重複拒否、配布本体によるPDF/Office処理を確認。VS Codeの実画像ツール出力、複数画像/JPEG、TXT添付の本体統合、会社環境・未認証初回導線は未完了。実用化目標全体は継続中で、このチェックポイントを完了宣言とはしません。

Run.cmdで専用の接続設定を生成し、Relay・Edge・VS Codeを起動します。追加モデルAPIへの接続は設定しません。既存の通常VS Codeプロファイルは変更しません。

文字列引数のjson-string、M365のScriptorコード行読取、終端付き最終回答V2、120000文字の送信前上限を追加しました。

入力欄が作り直される間の待機、Windowsの実保存先を使うワークスペース起動、会話要約の搬送形式保持も追加しています。コード記号の変化への対策として入力JSONのUnicode表現と末尾の確認文を導入しました。文字数上限はこれらを含む実際の送信文字数で判定します。

VS Code用の入力予算を28,000へ調整しました。限定した3ファイル課題の2回で約2分の結果を得ていますが、確認漏れが残り、一般的な完走性は検証中です。実送信の120,000文字上限は維持しています。

短文の往復は成功していますが、複数ファイル課題では、過去の試行に文字列変化による構文エラーや確認不足がありました。現修正版の長文編集は検証中です。配布検査の成功だけで成果物の正しさや実用化完了を示すものではありません。

無介入の安定性、高速化、会社端末・テナントでの利用は検証継続中です。補助あり課題の成功を実用化の完了とは扱いません。

---

# M365Relay 0.2.15

0.2.2を基準に、実M365＋VS Codeでの検証中に判明した互換性修正を累積で反映した版です。
配布ZIPはGitHub Actionsでコミット済みソースから生成し、Node.js 22.23.2 Windows x64と全文LICENSEを同梱します。
`release-manifest.json`の`sourceRevision`はビルド対象の実コミットです。利用者向けにはマージ後mainの成功したCIから取得してください。
ZIP名は`M365Relay-0.2.15-win-x64-<commit>.zip`です。

## 主な更新

- 入力欄のリンク化・非編集SPAN・`aria-hidden`装飾要素を実M365で確認し、本文の完全一致を保った読み取りへ対応。
- 長文はCDP `Input.insertText` 1回で入力し、再挿入せずDOM安定を待機。送信ボタン有効化も最大15秒待ち、送信直前に全文照合。
- DOM/内部例外の診断を本文非露出の安全なメタデータへ限定。
- PC再起動前の古い`bridge.lock`をOS起動時刻で判定し自動回収。PID再利用による誤判定を防止。
- 最終回答を`BRIDGE_FINAL`、ツール呼び出しを`BRIDGE_TOOL`のテキスト搬送形式へ対応。引用符・Windowsパス・Markdown・改行によるJSON破損を回避。
- M365が制御語の`_`を`\_`へ変える場合や、制御形式の改行を1行へ畳み込む場合に対応。
- 旧JSON形式の応答は後方互換として残し、Windows絶対パスの限定的な正規化のみ実施。
- 同一ユーザー要求内で全ツール最大12回、`run_in_terminal`最大3回のループガードを追加。
- Python・pypdf・PyMuPDF・pdftotext等を存在前提にせず、失敗した同じ依存関係を言い換えて繰り返さないようM365向け指示を追加。

## 更新

旧版をCtrl+Cで停止し、新しいZIPを別フォルダーへ展開して`Start-Bridge.cmd`を起動します。
起動表示の0.2.15を確認してください。既存設定・`token.txt`・専用Edgeプロファイルは保持します。
Setupの再実行やAPIキー変更は不要です。M365_RELAY_HOME / M365_BRIDGE_HOMEの指定は従来と同じ値を使います。
途中まで入力された依頼は手動送信せず、その旧タブを閉じてください。

## 検証範囲

ローカルでは`node --test`と`node scripts/check.mjs`を通過済みです。
実VS Code＋実M365で、通常会話の往復、長文一括入力、送信待機、VS Code標準ツールの呼び出し実行までは確認しました。
一方、PDF本文抽出のようにVS Code側の利用可能ツール・依存関係に左右される複数ラウンド作業の安定完走は継続検証中です。

入力が欠けた場合は再挿入せず送信を停止し、本文を含まない診断のみを返します。
送信済み・結果不明の要求は自動再送しません。

実M365のDOM互換性は[dom-compatibility.md](dom-compatibility.md)を参照してください。
