# 次セクションへの依頼文 / 2026-09-09 チェックポイント

以下を次のセクションの依頼として使用してください。

## 目的と現状

M365Relay（C:\Users\yuuki\M365Relay、https://github.com/minimo162/M365Relay）を、誰でも設定・実行できる実用的なアプリにする作業を続けてください。まずこの文書とIssue #30、#27、最新チェックポイントを読み、Gitと公開版を再確認してください。全体目標は未完了です。

起動結果を実際に確認するPR #33はsquashマージ済み。実装コミットは bde26a5ba898ecc4e28d7489bb593c079f4230ba、main CI 34273444273は成功。191テスト、配布11件、更新13件の検証を通過しています。作業ブランチcodex/desktop-launch-statusはローカル・リモートとも削除済みです。

コードのバージョンは0.2.32ですが、このチェックポイント時の公開ZIPはv0.2.31です。0.2.32の公開は未実施なので、マージ済みと配布済みを区別してください。

## 実機の確認結果

- VS Code更新は完了し1.136.2になりました。起動ウィンドウの観測902ms、同じウィンドウへのBridge.cmd再オープン3345ms・exit 0。旧更新待ちは解消しています。
- B：sample.txtから3行のsummary.mdを作成して読み戻し、3行・主要事実・元ファイル不変を確認しました。ただし初回はEdge終了によるcdp_unavailableがあり、未送信を確認して再試行しました。最終回答の画面には改行が文字列のまま表示される問題も残っています。
- C：summary.mdの2行目だけを指定内容に変更。他の行とsample.txtが不変であることをバイト比較で確認しました。
- D：special.txtの完全コピーは失敗。元217bytesに対しcopy.txtは215bytesで、末尾CRLFが欠落しています。Get-FileHashが使えなかったという応答がありましたが、実際の端末エラー原因は未調査です。
- Dの最終回答はm365_response_invalid。その後のAutopilotによる3回の再試行はduplicate_requestで拒否され、再送防止は動きました。取得した応答DOMには引用・記号の変化とフレーム末尾の余分な文字がありました。生成時の誤りかDOM取得・表示の問題かは未確定です。

## 次に実施すること

1. Dの端末結果と応答取得処理を確認し、コピーの末尾改行欠落、ハッシュ取得失敗、最終回答の形式崩れを切り分けてください。Bの文字列としての改行表示も調べてください。
2. 正当なバックスラッシュ・引用・コードブロックを壊す一括置換や、不明な末尾文字の無条件削除で検証を緩めないでください。原文と抽出結果のどの段階で変化したかを確認してから修正してください。
3. 原因に応じた修正後、同じDの依頼を実VS Codeで再実行し、ハッシュ・バイト完全一致、読み戻し、最終表示、引用した指示を実行していないことを確認してください。手作業によるファイル修復をエージェントの成功として扱わないでください。
4. Issue #30の受入を更新してください。Issue #27の圧縮待ち時間、企業環境での導入可否、全体の実用性は引き続き未完了です。公開が必要ならmain CI由来の成果物を使い、公開URLからの再取得まで検証してください。

## ローカル証跡（Git管理外・削除しない）

- `.local/startup-acceptance/first-launch.json`、`reopen.json`：起動と再オープン。
- `.local/acceptance-text/stage-b.json`、`stage-c.json`、`summary-before-c.md`：B/C結果。
- `.local/acceptance-text/home/workspace`：元ファイルと結果。`before.json`に元ハッシュ。
- `.local/acceptance-text/check-d-difference.mjs`：Dの差分確認。
- B応答：`2c2d2382-1a3e-4eef-9361-8bd53b19d330.json`（同ディレクトリ）。
- Dのハッシュ比較ツール要求：`e5aa8ae1-453a-491b-b873-18db0c9b77d4.json`。
- D失敗要求ID：`7c525562-dfaf-472a-bf03-ac435afcde83`。調査用`inspect-failure.mjs`と`copy-failure.mjs`があります。コピー後のclipboard.readTextはタイムアウトし、元応答の取得は未確認です。クリップボード権限の強制付与はしないでください。
- 試験サーバーのexec session 31762はチェックポイント作成時にstop入力でexit 0を確認しました。試験用ブラウザタブの残存状態は再確認が必要です。全タブを一括で閉じないでください。

## 継続する制約

- 既存M365 Copilot契約と画面操作/CDPだけで実現。新規モデルAPI契約は追加しません。業務ツール実行はVS Codeが担当します。
- 新規チャットごとにGPT-5.6 Think Deeperを選択。入力256000 tokensとM365画面120000文字は別の制約です。ツール・履歴はTXT添付を利用します。
- 専用VS Code設定：フォント16、zoom 1、ワークスペース信頼off、Autopilot Preview既定。累積ツール回数制限は追加せず、重複送信防止と個別タイムアウトを維持します。
- 小型Python同梱を継続。python-pptxとlxmlは同梱しません。現在はopenpyxl、et_xmlfile、pypdf、pypdfium2、Pillowが対象です。
- 実際のAUD教材PDFと本文はM365へ再送しないでください。検証には合成資料を使います。
- PID352の停止は以前の自動承認審査で拒否されています。別経路で停止しないでください。Windowsのセキュリティ回避やユーザー作業の終了も行わないでください。
- ユーザーファイルと.local証跡を保存し、Obsidianのプロジェクト記録を参照・更新してください。完了報告ではローカル検証、実機確認、CI、公開状態を区別してください。
