# 実体参照とコード記号の境界観測

状態: partial。根本修正未完了。

実M365で非実行のecho_sourceツールを指定し、矢印と文字列リテラル `"&gt; &lt; &amp;"` を含む短いコードをそのまま返す試験を2回行った。今回は所有要求タブを閉じる直前にassistant DOMのtextContent、innerHTML、indexed code行を取得し、backendが返した候補と復号後引数を保存した。業務ツール実行・ファイル書込は行わない。

結果は2回とも不一致。1回目は矢印を保持したがリテラルが `"> < &"` に変化。2回目はリテラルを保持したが矢印が `=&gt;` に変化した。いずれもindexed code行のtextContentに既に同じ変化があり、RelayのJSON復号後に初めて変わったものではない。1回目のinnerHTMLには通常の `&gt;` エスケープ、2回目には `&amp;gt;` があり、textContentによる読み取りはそのDOMの文字列と整合する。

この証拠が絞れるのは「回答DOMができるまでのどこか」であり、M365の入力処理、モデル生成、回答レンダラーのどこかを確定したわけではない。

追加の検証専用指示でJSON-string内の&/< />をUnicodeエスケープするよう求めた2回も、指定表現が出力に使われず同様の不一致となった。指示には `&gt;` を5文字とする誤記があった（正しくは4文字）。この試験を方式自体の不可能性の証拠とはしない。製品プロンプトや復号器には採用していない。

記録: `.local/input-events/entity-boundary-results.json`、`entity-unicode-results.json`。前者の実行session96791、後者71745は正常終了。通常サーバーPID36956/session84010を再起動せず、待受が空いている状態で直接backendの画面経路を逐次検証した。

次は入力側のJSON文字列でも実体参照を構成する記号が明示的なUnicodeエスケープになるようにした比較を検討する。コード記号と正当なリテラルの両方が一致することが必要で、一律エンコード/デコードや矢印だけの成功で解決扱いにしない。

## 入力JSONのUnicode表現比較

同じ短い課題で、入力payloadのJSON表現だけについて&/< />をUnicodeエスケープに変更した2回は、矢印と実体参照リテラルの両方が完全一致した。出力への追加指示は使っていない。記録は `.local/input-events/entity-input-escaped-results.json`、実行session4907は正常終了。従来表現の2回不一致に対し有望だが、少数の比較で長文編集の信頼性を証明しない。

この表現をprepareRequestに実装した。payloadの値は変更せず、JSON.stringify後の表現のみ変える。120000文字上限はエスケープ展開後に判定する。単体117件、構文25JS/4JSON成功。回帰では日本語・emoji・HTMLタグ・実体参照・文字としてのUnicodeエスケープを含むJSONが元payloadへ完全復元されることと、展開による上限超過を検査した。

常駐サーバーへの反映、配布版更新、実VS Code長文編集の比較は未実施。根本原因の詳細な内部レイヤーと長期再現率も未確定。

## 常駐サーバー反映後の再発

実Edge合成検証18件成功後、旧サーバー終了を確認して813735fを常駐サーバーへ反映した。新PID42708、exec57189。既存と同じ要求の試験は台帳が409 duplicate_requestで拒否し、画面送信しなかった。台帳は消去せず、識別子を持つ新しい比較試験を作った。

新規試験はHTTP200、11104msだが不一致。期待リテラル `"&gt; &lt; &amp;"` に対して `">&lt; &amp;"` が返り、最初の実体参照と空白が変化した。矢印は保持した。記録は `.local/input-events/entity-server-fixed-fresh-result.json`。試験session8183は正常終了。

入力Unicode表現の2回成功を一般化できないことが分かった。この変更はJSONの値を保つが、モデルを含む往復の文字列忠実性を保証しない。現時点で修正済みとは扱わず、長文課題の再実行より短い再現例で出力取得境界をさらに検証する。配布版は未更新。

## コードコピー経路との比較

入力Unicode表現の短い試験を2回実施し、回答内の「コードをコピー」ボタンが渡すtext/plainとDOM抽出候補を比較した。診断用の所有タブ内でclipboard.writeText/writeを一時的に捕捉して比較文字列だけを採取し、元のメソッドを復元してタブを閉じた。システムのクリップボードには検証文字列を書いていない。ブラウザーの権限設定変更なし。

1回目はDOM・コピーとも完全一致。2回目はどちらもリテラルが `"> < &"` になり不一致。復号後のコピー引数とDOM引数は2回とも一致した。したがって今回の不一致は、表示用textContentだけの読取問題としてコピー経路に変更すれば解消するものではない。モデル生成とM365内部処理のどちらかはなお未確定。

記録は `.local/input-events/entity-copy-results.json`、スクリプトは `entity-copy.mjs`。実行session22380は正常終了。コピー経路を製品の既定に採用する変更はしていない。次は送受信表現と検証方法を検討する。

## 末尾の搬送確認文

入力JSONの後ろに、Unicodeエスケープの具体例（矢印と実体参照リテラルの違い）を示す検証用指示を置いた3回は、すべて完全一致した。今回は回答DOMのjson-string自体がUnicodeエスケープを使っていた。記録は `.local/input-events/entity-tail-results.json`、session14362正常終了。

prepareRequestに末尾確認文を組み込んだ。検証用はecho_source専用のツール出力指示だったが、製品版はtool_choice/response_formatと通常の最終回答も維持する文言に調整したため、製品版そのものの実M365再検証は残る。確認文を含めた総文字数に120000上限を適用する。単体117件と構文25JS/4JSON成功。短い3回の一致を長文編集での信頼性とは扱わない。

## 実装版の短文実M365検証

808f8f2のprepareRequestを追加指示なしで使用した同じ試験3回は完全一致。backend時間11367/12878/11179msで、いずれも回答DOMにUnicodeエスケープを確認した。`tool_choice:none`の通常の最終回答も9507msで指定した数字に完全一致。

記録: `.local/input-events/entity-production-footer-results.json`、`footer-final-result.json`。試験session73321正常終了。既存サーバーPID42708の終了を確認してから更新し、現サーバーPID40848、exec31316で待受。ソース検証版であり配布ZIPではない。

短い文字列での安定性は改善した兆候があるが、まだ実VS Codeの長い要求・複数ファイル編集での再検証が必要。全体の速度、圧縮の頻度、編集後検証の正しさ、配布導線の完了条件は残る。
