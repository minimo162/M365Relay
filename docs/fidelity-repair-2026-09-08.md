# 文字忠実性の修正と実機再検証

状態：partial。実用化・高速化の目標は継続中。正式配布版ではない。

## 実装

- 文字列引数に `json-string` を追加。JSONとして一度だけ復元し、先頭末尾の空白・改行・制御語を含む値を保つ。旧形式の意味は変更しない。
- 応答をコードブロックで出すよう指示。外側の完全なコードフェンスだけを処理し、地の文から有効そうな断片を推測抽出しない。
- 実M365で観測したScriptorの `data-line-index` を持つコード行を読む。装飾行番号を本文に含めず、0開始の連続性と終端を確認する。途中省略・欠落・終端不一致は受理しない。終端の後に追加された空の表示行には対応。
- 最終回答に終端必須の `BRIDGE_FINAL_V2` を追加。旧最終回答の本文にある文字列を終端と誤解釈しない。
- 搬送用プロンプト全体を120,000文字以内に制限。旧180,000文字設定でも超過を送信前に拒否し、本文を切り捨てない。
- 実行結果が返っているのに「M365自身ではPCを操作できない」として中断しないよう、VS Codeとの役割分担を明確化。

## 証拠

- ローカル102 tests PASS。
- 実Edge 152.0.4191.66、模擬DOMの17 tests PASS。これはM365本番の通し試験と別。
- 実M365の既存応答を新DOM読取で再読し、Windowsパスの完全一致を確認。再送なし。
- 新しい実要求でWindowsパス41文字、複数行の置換文字列132文字が完全一致。
- 空白・引用符・制御語を含む難しい合成文字列では、モデルが余分な改行を生成した例がある。すべての文字列の安定性を証明していない。
- 別の入力形式による合成試行には、応答拒否、過剰なエスケープ、送信前の空入力による停止もある。成功例だけを全試行の結果として扱わない。
- `BRIDGE_FINAL_V2` の最新実機スモークはHTTP 200、10,072ms、指定した数字に完全一致。

## 実VS Codeでの修正課題

前回の検証物を保存し、同じ初期不具合を持つ別フォルダー `workload/fidelity-v1` を作成した。
選択ツール群はread/search/edit（実要求20ツール）。GitHubへのサインイン、追加モデルAPIは使用していない。

初回は約81秒で4ファイルを読み、2ファイルを改行付きで変更したが、モデルが実行不能と説明して中断。成果物判定は不合格。
実際の検証結果を一度返し、継続指示を明確化した後、残るreportを修正し、変更後の読取・最終報告まで到達した。固定判定はPASS。仕様と判定ファイルは変更していない。
これは**補助ありの課題成功1件**であり、無介入の安定完走や一般業務の成功ではない。

このVS Code試行は最終回答V2を導入する直前の応答形式で行った。V2は上記の独立した実機スモークとローカル回帰で確認し、最新ソース全体の無介入通しは次回の課題とする。

## 速度の基準値

- 実M365への要求15回、入力49,740〜61,375文字。
- バックエンド処理時間の合計173,052ms、中央値11,465ms。
- 最初の要求から最終イベントまで約366秒。この間には観測者の確認・プロンプト調整・追加入力の待ち時間が含まれる。
- 合計173秒もM365生成だけの時間ではなく、タブ生成・入力・安定待ち・後処理を含む。
- 待ち時間を含む経過時間をモデル速度に読み替えず、また失敗時の短さを高速化の成功としない。

ローカルの証拠は `.local/fidelity-roundtrip/`。認証情報を含むVS Codeプロファイルや生ログは公開しない。

## 次に必要な作業

1. 最新ソースで同一課題を無介入で再実行し、反復成功率を確認する。
2. 入力が空へ戻る症状の原因を特定する。入力直前・直後のフォーカスと文字数の計測を用意済みだが、根因は未確定。
3. 正常動作の時間内訳を取り、往復数・不要なツール定義・待機を削減する。
4. 接続キー・JSON編集を利用者に要求しない初回設定を実装・配布物で検証する。

失敗の隠蔽、入力の再挿入・結果不明要求の自動再送、単体テストを実機成功と扱うことはしない。
