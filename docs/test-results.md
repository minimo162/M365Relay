# 検証範囲

## 0.2.16候補の現在地（2026-09-08）

ローカルの単体117件、構文25 JavaScriptモジュール・4 JSON、実Edge上の合成画面18件が成功しています。実M365の結果とは区別します。

`eb4becedbfc0`由来の候補ZIPは、同梱Nodeのみを用いるWindows配布8検査に成功しました。ソースを直接動かす観測サーバーでの結果を、ZIPの実M365通し成功とは扱いません。この候補のGitHub CI・公開は未実施です。

実M365では11.3万文字の送信3回を確認しました。コード記号と実体参照の取り違えが見つかり、入力JSONのUnicode表現と末尾の出力確認文を追加しています。追加後の短い文字列往復3回と通常の最終回答は成功していますが、任意のコード・長文の文字列忠実性は保証できていません。

実VS Codeの固定3ファイル課題では、ある試行は成果物の検査に合格しても再読を省略し、別の試行は再読後にも構文エラーを残しました。後者は約7分20秒かかっています。最新修正版の同じ課題は検証中で、実用性・高速性・無介入完走は未達です。利用者による初回操作、会社端末・テナントの適合も未確認です。

以下は過去の版の記録です。

## 初版から継承した検証

2026-09-07、Linux / Node.js v22.16.0で元の0.1.0を再実行し、57 tests / 57 passed / 0 failedを確認しました。
HTTPは実際のlocalhostサーバー、M365は決定的な模擬応答、DOM/CDPは模擬環境です。

## 0.2.0で追加する検証

同梱ランタイムの版とハッシュ固定、利用者のPATH Node・起動時ダウンロードへの非依存、ライセンス同梱、
配布manifestとソース由来の記録、配布時の秘密データ除外をテストに追加しています。
実行結果の正本は各commitのGitHub Actionsログです。未実行のCIを成功扱いしません。

Windowsでは実際の同梱Nodeによる起動、PATH除去、空白パス、繰り返し初期化、ランタイム欠落、
ソース/ランタイム破損の拒否を検査します。これはWindows配布契約の検証であり、M365の実機検証ではありません。

## 0.2.2の検証

既存63件と追加スキーマ11件の計74件をLinux/Windowsで実行します。
ローカルの構文検査（19モジュール・4 JSON）と74件は成功しています。
実ブラウザーの模擬画面回帰13件は別のCIジョブで実行し、配布ジョブの前提とします。
Windows配布試験は8項目で、既存の6項目に設定保持と配布文書・版・コミット整合の確認を追加しています。
CIの成功は対象コミットのActionsログで確認してください。
模擬画面でのブラウザー成功と、実M365の通し成功は別です。

## まだ証明していないこと

実VS Code・Edge・M365の通し動作、M365のJSON生成安定性、実画面の文字忠実性、テナントのDOM互換性、
VS Code標準ツール全種類のスキーマ互換性、実ブラウザーの停止、財務業務の意味品質、性能、社内承認、JSON Schema全面準拠。
