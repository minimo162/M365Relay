# 配布版の必須確認を含む固定課題

状態: この限定課題は合格。アプリ全体の実用化目標はpartial。

507b89033480の配布ZIPをRun.cmdで起動し、同じ仕様・初期欠陥を持つrequired-v8で実VS Codeから依頼した。追加指示なし。サブエージェントや実行ツールは使わず、読取・編集をVS Codeのツールで行い、実行検査は観測側で別途行う条件。

19:57:24.938Z〜19:59:42.480Z、約137.5秒（実画面2分17秒）。初期4読取、3編集、変更後3読取、最終回答まで全11要求が成功。最終報告だけでなく、展開した実行履歴でnormalize/calculate/reportの変更後読取をそれぞれ確認した。

固定verify成功。追加の単価3・数量0.5でtotal1.5も成功。SPEC/verifyのSHA256は初期値と一致。観測者は実装を修正せず、失敗のフィードバックも与えていない。コード記号の変化による構文エラーや120000文字上限超過なし。

メタデータログ: `.local/required-check-package/required-cli.jsonl`。サーバーPID44556、exec5353は試験後も待受。配布候補は `dist/required-check-candidate/M365Relay-0.2.16-win-x64-507b89033480.zip`。

この1回を任意作業の信頼性へ一般化しない。未サインインからの全初回操作、長い継続作業、独立チャット窓の入力問題、他の業務や入力形式、公開・CIの確認は残る。
